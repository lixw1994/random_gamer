const randomGenerator = require('./module/random_gamer');

module.exports = {
    genSimpleRandomGamer: randomGenerator.genSimpleRandomGamer,
    genHashRandomGamer: randomGenerator.genHashRandomGamer,
};